diff --git a/lib/I18n/translations/english.yaml b/lib/I18n/translations/english.yaml
index 6d91063b..b9c79946 100644
--- a/lib/I18n/translations/english.yaml
+++ b/lib/I18n/translations/english.yaml
@@ -10,6 +10,7 @@ STR_ENTERING_SLEEP: "Going to sleep"
 STR_BROWSE_FILES: "Browse Files"
 STR_FILE_TRANSFER: "File Transfer"
 STR_SETTINGS_TITLE: "Settings"
+STR_BUILD_MONITOR: "Build monitor"
 STR_CONTINUE_READING: "Continue Reading"
 STR_RESUME: "Resume"
 STR_NO_OPEN_BOOK: "No open book"
diff --git a/src/activities/settings/SettingsActivity.cpp b/src/activities/settings/SettingsActivity.cpp
index 61a9e5e0..34e449f0 100644
--- a/src/activities/settings/SettingsActivity.cpp
+++ b/src/activities/settings/SettingsActivity.cpp
@@ -25,6 +25,9 @@
 #include "SettingsList.h"
 #include "StatusBarSettingsActivity.h"
 #include "TextSettingsActivity.h"
+#if defined(GPIO_BUILD_MONITOR)
+#include "MonitorActivity.h"
+#endif
 #include "activities/network/WifiSelectionActivity.h"
 #include "activities/util/IntervalSelectionActivity.h"
 #include "components/UITheme.h"
@@ -84,6 +87,9 @@ void SettingsActivity::rebuildSettingsLists() {
                             SettingInfo::Action(StrId::STR_REMAP_FRONT_BUTTONS, SettingAction::RemapFrontButtons));
   }
   systemSettings.push_back(SettingInfo::Action(StrId::STR_WIFI_NETWORKS, SettingAction::Network));
+#if defined(GPIO_BUILD_MONITOR)
+  systemSettings.push_back(SettingInfo::Action(StrId::STR_BUILD_MONITOR, SettingAction::BuildMonitor));
+#endif
   systemSettings.push_back(SettingInfo::Action(StrId::STR_KOREADER_SYNC, SettingAction::KOReaderSync));
   systemSettings.push_back(SettingInfo::Action(StrId::STR_OPDS_SERVERS, SettingAction::OPDSBrowser));
   systemSettings.push_back(SettingInfo::Action(StrId::STR_CLEAR_READING_CACHE, SettingAction::ClearCache));
@@ -378,6 +384,15 @@ void SettingsActivity::toggleCurrentSetting() {
           LOG_ERR("SETTINGS", "OOM: KeyboardLayoutsActivity");
         }
         break;
+#if defined(GPIO_BUILD_MONITOR)
+      case SettingAction::BuildMonitor:
+        if (auto activity = makeUniqueNoThrow<MonitorActivity>(renderer, mappedInput)) {
+          startActivityForResult(std::move(activity), nullptr);
+        } else {
+          LOG_ERR("SETTINGS", "OOM: MonitorActivity");
+        }
+        break;
+#endif
       case SettingAction::None:
         // Do nothing
         break;
diff --git a/src/activities/settings/SettingsActivity.h b/src/activities/settings/SettingsActivity.h
index 824d294c..015562dd 100644
--- a/src/activities/settings/SettingsActivity.h
+++ b/src/activities/settings/SettingsActivity.h
@@ -25,6 +25,9 @@ enum class SettingAction {
   DownloadFonts,
   TextSettings,
   KeyboardLayouts,
+#if defined(GPIO_BUILD_MONITOR)
+  BuildMonitor,
+#endif
 };
 
 struct SettingInfo {
