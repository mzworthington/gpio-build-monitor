# Automatic Analysis. Wildcard patterns are not allowed.
# Directory entries in sonar.sources are recursive; sonar.exclusions are not.
# Omit the CrossPoint git submodule by listing first-party trees only.
# CI scan uses sonar-project.properties; disable Automatic Analysis after the first CI scan.
sonar.sources=monitor/api,monitor/device/web,monitor/device/pi,monitor/device/menu-bar,monitor/device/eink/apps,monitor/device/eink/host_test,monitor/device/eink/include,monitor/device/eink/src,monitor/device/eink/test,infra,test,bin,.github
